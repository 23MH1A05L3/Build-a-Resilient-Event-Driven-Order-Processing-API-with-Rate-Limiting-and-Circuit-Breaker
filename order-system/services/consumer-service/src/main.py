import logging
from consumers.order_consumer import start_consuming_orders

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
)

if __name__ == "__main__":
    start_consuming_orders()
